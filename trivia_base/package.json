{
    "name": "trivia-base-account-deletion",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=20.0.0",
        "npm": ">=10.0.0"
    },
    "scripts": {
        "preinstall": "node -e \"if(process.version.split('.')[0].slice(1) < 20) throw new Error('Node.js 20+ is required. Current version: ' + process.version)\"",
        "setup": "./scripts/setup-node.sh",
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && echo 'Build complete. Push to GitHub to deploy.'"
    },
    "dependencies": {
        "firebase": "^12.3.0",
        "vue": "^3.5.13",
        "vue-toastification": "^2.0.0-rc.5"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^5.2.1",
        "typescript": "~5.7.3",
        "vite": "^6.1.0",
        "vue-tsc": "^2.2.2"
    }
}
